type UserMetrics{
    _id: ID
    user: String
    count: Int
    weight: Float
}

type TotalMetrics{
    count: Int
    weight: Float
}

type Query {
    fileUserMetrics: [UserMetrics]
    fileGlobalMetrics: TotalMetrics
}