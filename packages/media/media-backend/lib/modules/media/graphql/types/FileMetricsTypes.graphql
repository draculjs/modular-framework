type UserMetrics{
    _id: ID
    user: String
    count: Int
    weight: Int
}

type TotalMetrics{
    count: Int
    weight: Int
}

type Query {
    fileUserMetrics: [UserMetrics]
    fileGlobalMetrics: TotalMetrics
}