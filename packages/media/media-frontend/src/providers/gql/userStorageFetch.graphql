query userStorageFetch{
    userStorageFetch{
        id
        user {
            name
            username
            id
        }
        capacity
        usedSpace
        maxFileSize
        fileExpirationTime
        deleteByLastAccess
        deleteByCreatedAt
    }
}
