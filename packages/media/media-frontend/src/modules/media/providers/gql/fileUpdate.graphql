mutation fileUpdate($id: ID!, $description:String, $tags:[String], $expirationDate: String){
    fileUpdate(id: $id, input: {description: $description, tags: $tags, expirationDate: $expirationDate}){
        id
        filename
        description
        tags
        mimetype
        type
        extension
        relativePath
        absolutePath
        size
        url
        createdAt
        createdBy{
            user{
                id
                name
                username
            }
            username
        }
        lastAccess
        expirationDate
    }
}

