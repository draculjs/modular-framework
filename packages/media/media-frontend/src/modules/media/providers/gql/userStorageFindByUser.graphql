query userStorageFindByUser {
    userStorageFindByUser {
        id
        user {
            name
            username
            id
        }
        capacity
        usedSpace
        maxFileSize
        fileExpirationTime
        deleteByLastAccess
        deleteByCreatedAt
    }
}
