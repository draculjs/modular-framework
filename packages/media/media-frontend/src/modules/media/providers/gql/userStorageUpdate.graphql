mutation userStorageUpdate($id: ID!, $name:String!, $capacity:Float!, $usedSpace:Float, $maxFileSize: Float){
    userStorageUpdate(id: $id, input: { name: $name, capacity: $capacity, usedSpace: $usedSpace, maxFileSize: $maxFileSize}){
        id
        name
        capacity
        usedSpace
    }
}