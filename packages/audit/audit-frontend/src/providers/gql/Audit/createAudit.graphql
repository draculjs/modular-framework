mutation createAudit($action: String!, $resource: String!, $description: String){
    createAudit(action: $action, resource: $resource, description: $description){
        id
        user{
                id
                username
            }
        action
        resource
    }
}

