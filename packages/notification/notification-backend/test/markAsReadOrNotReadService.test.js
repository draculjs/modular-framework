import {markAsReadOrNotReadService} from '../src/services/NotificationService'
import assert from 'assert'

describe('markAsReadOrNotReadService',  () => {

    it('Should return an ObjectId in the markAsReadOrNotReadService  ', async () => {

      /*   let idNotification = "5fa3f45b3f5b3c44d002c7b5"
        let readValue = true

        let docNotification = await markAsReadOrNotReadService(idNotification, readValue)

        console.log('docNotification'+docNotification)

        assert.strictEqual(typeof docNotification.id, "string") */
        
    });
    
});
