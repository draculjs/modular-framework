query settingsFind($id:ID!){
    settingsFind(id:$id){
        id
        key
        value
        label{
            en
            es
            pt
        }
        type
        options
        reject
    }
}
