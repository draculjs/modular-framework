query settingsFind($id:ID!){
    settingsFind(id:$id){
        id
        key
        text
        value
        label{
            en
            es
            pt
        }
        type
        options
        regex
        entity
        field
    }
}
