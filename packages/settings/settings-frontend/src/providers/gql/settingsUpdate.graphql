mutation settingsUpdate($id: ID!, $value:String){
    settingsUpdate(id: $id, value: $value){
        id
        key
        value
        label{
            en
            es
            pt
        }
        type
        options
        regex
        entity
        entityValue
        entityText
    }
}

