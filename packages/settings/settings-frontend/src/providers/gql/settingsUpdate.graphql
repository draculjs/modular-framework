mutation settingsUpdate($id: ID!, $key:String!, $value:String, $label:MultiLangInput!){
    settingsUpdate(id: $id, input: {key: $key, value: $value, label: $label }){
        id
        key
        value
        label{
            en
            es
            pt
        }
        type
        options
        regex
        entity
        field
    }
}

