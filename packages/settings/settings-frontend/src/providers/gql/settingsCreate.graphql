mutation settingsCreate($key:String!, $value:String, $label:MultiLangInput!){
    settingsCreate(input: {key: $key, value: $value, label: $label }){
        id
        key
        value
        label{
                en
                es
                pt
            }
        type
        options
        regex
        entity
        field
    }
}

