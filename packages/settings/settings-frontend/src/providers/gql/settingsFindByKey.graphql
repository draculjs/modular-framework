query settingsFindByKey($key:String!){
    settingsFindByKey(key:$key){
        id
        key
        value
        valueList
        group
        label{
            en
            es
            pt
        }
        type
        options
        regex
        entity
        entityValue
        entityText
        prefix
        suffix
    }
}
