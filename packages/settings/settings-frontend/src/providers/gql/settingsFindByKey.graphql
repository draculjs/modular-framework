query settingsFindByKey($key:String!){
    settingsFindByKey(key:$key){
        id
        key
        value
        group
        label{
            en
            es
            pt
        }
        type
        options
        regex
        entity
        entityValue
        entityText
    }
}
