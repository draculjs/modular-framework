type RefreshToken {
    token: String!
    expiryDate: String!
}

type AuthResponse {
    token:String!
    refreshToken: RefreshToken!
}

type RefreshResponse {
    token:String!
}

type Query {
    me: User
}

type Mutation {
    auth(username: String!, password:String!): AuthResponse!
    refreshToken(token: String!, expiryDate: String!): RefreshResponse!
    apikey(userid: ID!): AuthResponse!
}