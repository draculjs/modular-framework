mutation roleCreate($name:String!, $childRoles: [ID], $permissions: [String]){
    roleCreate(input: {name: $name, childRoles: $childRoles, permissions:$permissions }){
        id
        name
        childRoles
        permissions
        readonly
    }
}

