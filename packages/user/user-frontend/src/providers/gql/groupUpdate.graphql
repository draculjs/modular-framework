mutation groupUpdate($id: ID!, $name:String!, $color:String, $users: [String]){
    groupUpdate(id: $id, input: {name: $name, color: $color, users: $users }){
        id
        name
        color
        users{
            id
            username
        }
    }
}

