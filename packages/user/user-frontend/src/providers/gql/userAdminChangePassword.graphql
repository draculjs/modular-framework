mutation changePasswordAdmin($id: ID!, $password:String!, $passwordVerify:String!){
    changePasswordAdmin(id:$id, password:$password, passwordVerify:$passwordVerify){
        status
        message
    }
}
