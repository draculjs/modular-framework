query group($id:ID!){
    group(id:$id){
        id
        name
        color
        users{
            id
            name
            username
            email
            phone
            active
            avatarurl
        }
    }
}
